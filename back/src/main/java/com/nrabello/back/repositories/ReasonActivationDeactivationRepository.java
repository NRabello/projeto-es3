package com.nrabello.back.repositories;

import com.nrabello.back.domain.ReasonActivationDeactivation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReasonActivationDeactivationRepository extends JpaRepository<ReasonActivationDeactivation, Long> {
}
